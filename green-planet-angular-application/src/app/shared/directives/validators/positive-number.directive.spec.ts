import { NegativeNumberDirective } from './positive-number.directive';

describe('PositiveNumberDirective', () => {
  it('should create an instance', () => {
    const directive = new NegativeNumberDirective();
    expect(directive).toBeTruthy();
  });
});
